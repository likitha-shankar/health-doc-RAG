# Extra dependencies for the evaluation pipeline (CI only).
# Install with: pip install -r requirements.txt -r requirements-eval.txt
ragas==0.2.15             # LLM-judge evaluation framework
langchain-google-genai==2.1.8  # Google Gemini integration for Ragas LLM judge
